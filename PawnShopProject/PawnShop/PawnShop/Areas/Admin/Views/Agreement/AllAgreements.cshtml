@model IEnumerable<AllAgreementViewModel>

@if (Model.Count() == 0)
{
	ViewBag.Title = "No Agreements";
}
else
{
	ViewBag.Title = "All Agreements";

}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />


@if (!ViewData.ModelState.IsValid && ViewData.ModelState["Error"] != null)
{
	<div class="alert alert-danger" role="alert">
		@ViewData.ModelState["Error"].Errors.First().ErrorMessage
	</div>
}

<table class="table table-striped">
	<thead>
		<tr>
			<th>Goods</th>
			<th>Description</th>
			<th>Price</th>
			<th>Return Price</th>
			<th>Duration</th>
			<th>Interest</th>
			<th>Start Date</th>
			<th>End Date</th>
			<th>State</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{			
			<tr>
				<td>@item.GoodName</td>
				<td>@item.Description</td>
				<td>@item.Price</td>
				<td>@item.ReturnPrice</td>
				<td>@item.Duration</td>
				<td>@item.Ainterest</td>
				<td>@item.StartDate.ToString("dd/MM/yyyy")</td>
				<td>@item.EndDate.ToString("dd/MM/yyyy")</td>
				<td>@item.AgrreementStates</td>
				<td>
					<a asp-area="Admin" asp-controller="Agreement" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Detail</a>
				</td>
			</tr>			
		}
	</tbody>
</table>

